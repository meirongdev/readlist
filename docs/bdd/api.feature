# language: zh-CN
# 覆盖: FR-53 | NFR-4, NFR-14 | AC-P3
@P0
功能: 只读 API 与边缘限流
  作为访客或前端
  我想通过只读 API 取榜单、书详情与预设列表
  以便渲染页面而不引入任何写面

  场景: API 全部只读
    当 客户端请求任一 API 端点
    那么 仅支持 GET 读取
    而且 不存在任何可写端点（无 POST/PUT/DELETE 业务语义）

  场景: 预设列表端点
    当 客户端请求 GET /api/presets
    那么 返回全部公开预设（不含 visibility=internal 的榜）

  场景: 榜单端点
    当 客户端请求 GET /api/rankings/{preset}
    那么 返回按该预设排序的书目及证据等级
    而且 与 Web 榜单页数据一致

  场景: 书详情端点
    当 客户端请求 GET /api/books/{id}
    那么 返回该书得分拆解、数据来源、证据等级与标准版本

  场景: 边缘限流分档
    当 请求超过边缘限流阈值
    那么 返回 429 拒绝
    而且 页面与 /api/ 使用不同档位的限流

  场景: 外部 API 全挂时降级出榜
    假如 Google Books / OpenLibrary / HN Algolia 全部不可用
    当 用户请求榜单
    那么 系统用缓存数据继续出榜，不整站不可用
    而且 指标反映"外部数据已过期"

  @P1
  场景: 未知资源返回标准错误
    当 客户端请求不存在的书或预设
    那么 返回 404
    而且 响应不含任何书库内部细节
