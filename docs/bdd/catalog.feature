# language: zh-CN
# 覆盖: FR-25, FR-33, FR-52, FR-52b | 开放问题 Q2 | AC-P3
@P1
功能: 上榜书目
  作为访客
  我想在一个页面里浏览所有上过榜的书，并看到证据不足的书被如实标注
  以便不会被"数据不足硬凑出来"的书误导

  @P0
  场景: 书目页只收公开榜单的并集
    假如 当前 run 已发布
    当 访客打开上榜书目页
    那么 只出现上过公开榜的书
    而且 条目数严格少于全库 works 数

  @P0
  场景: 未上榜的书不在公开面上
    假如 某本书没有出现在任何公开榜里
    当 访客按 work_id 直接请求该书
    那么 返回 404
    而且 该书不出现在上榜书目页

  @P0
  场景: 上 internal 榜不等于公开
    假如 某本书只出现在 visibility 为 internal 的榜里（如 library-hygiene）
    当 访客按 work_id 直接请求该书
    那么 返回 404

  @P0
  场景: 不存在全库导出端点
    当 客户端请求任何一个「一次拿到全部 work」的端点
    那么 该端点不存在（404）
    而且 全库计数只在 /metrics 上报，不在内容 API 上

  场景: 上榜书缺维度时标注"数据不足"而不是被剔除
    假如 某本上榜书在若干维度上没有证据
    当 访客浏览上榜书目页
    那么 该书出现在页面上
    而且 显式标注"数据不足"以及缺哪几维
    而且 缺失维度不被显示成 0 分

  场景: 中文书单独成榜（开放问题 Q2 的倾向实现）
    假如 开放问题 Q2 已决定中文书单独成榜
    当 访客打开"中文书"榜
    那么 只包含语言为 zho 的书
    而且 使用适配中文书的数据源与半衰期配置
