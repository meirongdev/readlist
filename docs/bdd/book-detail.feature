# language: zh-CN
# 覆盖: FR-34, FR-45, FR-51, FR-54 | NFR-11, NFR-12 | AC-P3
@P0
功能: 书详情页
  作为访客
  我想打开一本书看它凭什么排这么高
  以便判断这个榜单值不值得信

  场景: 展示得分拆解
    当 访客打开某本书的详情页
    那么 页面展示每个维度的得分、原始值、语料内百分位
    而且 展示每个维度的数据来源
    而且 展示证据等级与 standard_version

  场景: 分数可追溯到原始来源
    当 访客查看某维度的分数
    那么 可以追溯到该分数的原始取值与来源
    而且 若该维来自外部 API，展示缓存响应时间

  场景: 阅读状态与"作者读过"
    假如 该书已被标记为已读
    当 访客打开详情页（expose_read_status=true）
    那么 页面展示"作者读过"标记

  场景: 弃读状态默认不公开
    假如 该书在"弃读"书架
    当 访客打开详情页
    那么 不展示弃读状态

  场景: 封面外链，不自托管
    当 访客打开详情页
    那么 封面图来自 OpenLibrary Covers 或 Google Books 外链
    而且 本站不保存或提供封面文件

  场景: 只提供外部阅读入口，不发书
    当 访客浏览详情页
    那么 页面提供出版社 / Google Books 外部链接
    而且 不提供下载、在线阅读或正文片段
    而且 不提供跳转到私有书库的链接

  @P1
  场景: 数据不足的书显示解释
    假如 某本书证据等级为 C
    当 访客在详情页查看
    那么 页面标注"数据不足"，并列出缺失的证据

  @P1
  场景: 标准版本演变可查
    假如 standard_version 从 1.0 升到 1.1
    当 访客查看某本书的分数
    那么 可切换到历史版本查看分数演变
