# language: zh-CN
# 覆盖: FR-40~46 | NFR-13, NFR-15 | AC-P0, AC-P3
@P0
功能: 阅读状态
  作为库主人或访客
  我想知道每本书我读没读过、并在榜单上按阅读状态筛选
  以便把客观排行变成自己的阅读队列

  背景:
    假设 快照 CronJob 已从 app.db 最小导出 reading.db
    而且 导出仅含 user_id=1 的记录，孤儿行已丢弃

  场景: 已读书展示徽章
    假如 某本书在 reading.db 中 read_status=1
    当 榜单或详情页渲染该书
    那么 显示"✓ 已读"徽章

  场景: 未读书不显示徽章
    假如 某本书没有任何阅读状态记录
    当 榜单渲染该书
    那么 不显示任何阅读状态徽章

  场景: 按阅读状态筛选
    当 访客选择筛选"全部 / 已读 / 未读"
    那么 榜单按该状态过滤
    而且 过滤不改变榜单排序逻辑

  场景: 书架承载扩展状态
    假如 某本书在"想读"书架
    当 榜单渲染该书
    那么 显示"☆ 想读"徽章
    假如 某本书在"精读"书架
    那么 显示"★ 精读"徽章

  场景: 阅读状态是 facet，不进分数
    假如 某本书被标记为已读
    当 计算 TBS 分数
    那么 其维度分与综合分与未标记时完全一致

  场景: 系统永不回写阅读状态
    当 任何用户访问任何页面或 API
    那么 系统不产生对 calibre-web 或 reading.db 的任何写操作
    而且 系统不存在任何写接口

  @P1
  场景: 公开性开关
    假如 配置 expose_read_status=false
    当 访客浏览任何页面
    那么 不展示任何阅读状态或"作者读过"标记

  @P1
  场景: 弃读书架默认不公开
    假如 某本书在"弃读"书架
    当 访客浏览公开站点
    那么 任何页面都不展示弃读状态
