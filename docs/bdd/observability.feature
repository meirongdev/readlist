# language: zh-CN
# 覆盖: FR-60~63 | AC-P4
@P1
功能: 可观测与降级
  作为运维
  我想在采集/评分/配额出问题时第一时间看见
  以便不让榜单静默过期

  场景: 静默过期可告警
    当 快照 / 采集 / 评分任一步失败
    那么 Prometheus 指标反映失败
    而且 最后一次成功运行的时间戳停止更新
    而且 该时间戳是告警依据

  场景: 配额与数据质量指标
    当 系统运行
    那么 暴露指标：外部 API 请求数、429 数、当日剩余配额估算
    而且 暴露各证据等级（A/B/C/D）书数
    而且 暴露孤儿行数与 LLM 低置信度标注数

  场景: 手动触发重算
    当 运维调用内部重算入口
    那么 系统基于最新 evidence 重算全部分数
    而且 无需等待夜间任务

  @P2
  场景: 孤儿行突增告警
    假如 某次运行的孤儿行数显著高于历史基线
    当 指标被检查
    那么 触发告警（提示 book id 漂移，有人在删书或重导入）
