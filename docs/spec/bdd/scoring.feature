# language: zh-CN
# 覆盖: FR-20~27 | NFR-10 | AC-P1, AC-P2
@P0
功能: 评分标准行为
  作为访客或库主人
  我想让分数可复算、缺数据不被当成 0 分、不可信来源被如实降级
  以便榜单"能看见怎么算出来的"

  场景: 同版本同快照可逐位复算
    假如 standard_version=1.0 且 evidence 快照与上次完全一致
    当 系统重跑评分
    那么 每本书的各维度分与综合分与上次逐位一致

  场景: 缺数据收缩到语料先验，不当 0 分
    假如 某本书无外部评分且无 HN 提及
    当 计算 A 与 C 维度
    那么 分数收缩到语料先验（贝叶斯）
    而且 分数不是 0

  场景: 口碑多源按人数加权合并
    假如 某本书在 Google Books 有 3,000 人评分、OpenLibrary 有 3 人评分
    当 计算 A 维度
    那么 两源分别计算后按各自评分人数加权合并
    而且 不做简单平均

  场景: 归一化用百分位而非 min-max
    假如 某本书 HN 提及数是极端 outlier
    当 归一化 C 维度
    那么 其余书的 C 分不被压到 0–3 区间
    而且 使用语料内百分位排名

  @blocked
  场景: 时效来源不可信记未知并降级
    假如 某本书 pubdate_source 为 mtime-fallback 或 unknown
    当 计算 F 维度
    那么 F 记为"未知"而非数值
    而且 该书不进入任何按 F 排序的榜
    而且 该书的证据徽章不受影响 —— F 不被任何一份榜加权，不参与排序的维度不参与评级

  @blocked
  场景: 时效按主题半衰期衰减
    假如 两本书同为 3 年前出版，但主题分别为"编译原理"（半衰期 25 年）与"LLM 应用"（半衰期 1.5 年）
    当 计算 F 维度
    那么 编译原理书的 F 分高于 LLM 应用书

  场景: HN 匹配宁少不多
    假如 某本书标题 ≤2 词且不在人工白名单
    当 匹配 HN 提及
    那么 该书不计算提及数
    假如 某条 HN 命中但作者姓氏与出版社不同现
    那么 该命中不计入提及数

  @P1
  场景: 出版社先归一再评级
    假如 出版社名为 "Packt Publishing Pvt Ltd"、"Packt Publishing Ltd"、"Packt Publishing"、"Packt" 之一
    当 计算 T 维度
    那么 四者先经 publisher_map 归并为同一出版社再取 tier

  场景: 版本化升级不覆盖历史
    当 评分标准从 1.0 升到 1.1
    那么 1.0 的历史分保留且可查询
    而且 升版伴随「20 本已知书的排名对比」评审材料
