# 参照件:homelab 仓库 oracle 侧是显式 kustomize 树,把 readlist.yaml 抄过去之后
# 还必须在那边目录的 kustomization.yaml 的 resources: 里登记 —— 漏登记 = 静默不生效。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: personal-services
resources:
  - readlist.yaml
